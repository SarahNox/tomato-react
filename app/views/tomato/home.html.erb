<div class="center jumbotron">
  <h1>Tomato-Clock</h1>
<h2>
  When its ready, it should display a pomodoro-clock
</h2>
<html>
<head>
    <script type="text/javascript">  

    var isTimerOn = false;
    var time = 0*60 + 5;
    var interval;
    var pause = 5*60 +0;
function countdown() {
    interval = setInterval(function() {
        var el = document.getElementById("countdown");
        if(time == 0) {
            el.innerHTML = "Tomatinatime!"; 
            // alert("Tomatinatime");   
            clearInterval(interval);
            return;
        }
        var minutes = Math.floor( time / 60 );
        if (minutes < 10) minutes = "0" + minutes;
        var seconds = time % 60;
        if (seconds < 10) seconds = "0" + seconds; 
        var text = minutes + ':' + seconds;
        el.innerHTML = text;
        time--;
    }, 1000);
}


   function stop() {
    isTimerOn = false;
    clearInterval(interval); 
    document.getElementById("toggle").innerHTML = "Start";
    alert("you crashed the pomodoro");
    pause();
   }


   function toggle() {
    if (!isTimerOn){
      isTimerOn = true;
      countdown();
      document.getElementById("toggle").innerHTML = "Pause";
  } else {
      isTimerOn = false;
      clearInterval(interval);
      document.getElementById("toggle").innerHTML = "Resume";
    }
   }

//   function pause() {
//     tomato = setInterval(function() {
//         var to = document.getElementById("countdown");
//         if(pause == 0) {
//             to.innerHTML = "time to start again!";    
//             clearInterval(tomato);
//             return;
//         }
//         var minutes = Math.floor( time / 60 );
//         if (minutes < 10) minutes = "0" + minutes;
//         var seconds = time % 60;
//         if (seconds < 10) seconds = "0" + seconds; 
//         var text = minutes + ':' + seconds;
//         to.innerHTML = text;
//         pause--;
//     }, 1000);
// }

    </script>
</head>
<body>
<div id='countdown'></div>
<button id="toggle" onclick="toggle()">Start</button>
<button id="stop" onclick="stop()">Stop</button>
</body>
</html>




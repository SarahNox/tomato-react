<!DOCTYPE html>
<html>
<head>
  <title>Tomato</title>
  <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.0/jquery.min.js"></script>
  <%= stylesheet_link_tag 'application' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <body class="pomodoros">
    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <ul class="tomato navbar-left">
          <p> pomodoro </p>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <div id="menu">
            <% if logged_in? %>
            <ul>
              <li class="topmenu"><a href="/">CLOCK</a></li>
              <li class="topmenu"><a href="/users">USERS</a></li>
              <li class="topmenu"><a href="">SETTINGS</a>
                <ul><li class="submenu">
                    <li><%=link_to "update user", edit_user_path(current_user) %></li>
                    <li class="submenu">
                    <li><%=link_to "update password", password_edit_path(current_user) %></li>
                </ul>
              </li><li class="topmenu"><a href="/logout" data-method="delete" >LOGOUT</a></li>
            </ul>
            <% else %>
            <ul>
              <li class="topmenu"><a href="/login">LOG IN</a></li>
              <li class="topmenu"><a href="/users/new">SIGN UP</a></li> 
            </ul>
            <% end %> 
          </div>
        </header>
      <div class="container">
        <% flash.each do |message_type, message| %>
          <div class="alert alert-<%= message_type %>"><%= message %></div>
        <% end %>
        <%= yield %>
      </div>
    </body>
  </html>
